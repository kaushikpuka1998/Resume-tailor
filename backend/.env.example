# Copy to .env. AI rewriting is optional — if these are blank, the system falls back to rule-based analysis only.
PORT=4000
CORS_ORIGIN=http://localhost:5173

# Optional: leave blank to disable AI rewriting (hybrid system still does rule-based analysis).
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
